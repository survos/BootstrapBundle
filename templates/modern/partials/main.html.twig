<!doctype html>
<html lang="en"
      data-layout="vertical"
      data-topbar="light"
      data-sidebar="dark"
      data-sidebar-size="lg" {# sm-hover, lg #}
      data-sidebar-image="none"
      data-preloader="disable">
